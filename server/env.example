# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_DATABASE=devs_repo

# JWT Configuration (for future use)
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=24h
